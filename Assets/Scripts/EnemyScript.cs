using UnityEngine;

public class EnemyScript : MonoBehaviour
{
    //Implement enemy behavior here
    //Add health and attack logic
    //Unsure if I'll end up making more than just turret enemies, but this is a good start
    //For turret enemies, use vector2 distance to check if player is within range and also to find the direction to fire in

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}