using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerKnightFormScript : MonoBehaviour
{
    //To Do:
    //Add a deflect 
    //Add a sword attack
    //Set start to on enable instead
    //maybe adjust the player movement script to allow for a different speed or jump height

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
